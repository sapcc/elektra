import { osImages } from './os_images'
import { imageMembers } from './image_members'

const available = osImages('available')
const suggested = osImages('suggested')

export { available, suggested, imageMembers }
