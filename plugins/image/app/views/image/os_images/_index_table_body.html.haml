- if @images.empty?
  %tr
    %td{ colspa: 5 } No #{@visibility} images available
- else
  - @images.each do |image|
    = render partial: "image/os_images/#{@visibility}/item", locals: { image: image }
