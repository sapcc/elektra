%tr{id: "image_#{member.id}"}
  %td
    - project_id_and_name(member.member_id)
  %td= member.status

  %td.snug
    -# - if current_user.is_allowed?("image:image_delete", {}) or current_user.is_allowed?("image:image_update", {})
    -#   .btn-group
    -#     %button.btn.btn-default.btn-sm.dropdown-toggle{ type: "button", data: { toggle: "dropdown"}, aria: { expanded: true} }
    -#       %span.fa.fa-cog
    -#
    -#     %ul.dropdown-menu.dropdown-menu-right{ role:"menu"}
    -#       %li= link_to 'Delete', plugin('image').image_access_control_path(id: member.id), method: :delete, data: { confirm: 'Are you sure you want to delete this member?', ok: "Yes, delete it", confirmed: :loading_status}, remote: true
