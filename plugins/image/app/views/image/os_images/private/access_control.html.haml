= content_for :title do
  Image Access Control

%div{ class: modal? ? 'modal-body' : '' }
  %table.table
    %thead
      %tr
        %th Target Project
        %th Status
        %th.snug
    %tbody
      - if @members.empty?
        %tr
          %td{ colspa: 5 } No Access given to other projects
      - else
        - @members.each do |member|
          = render partial: 'member_item', locals: { member: member }


.buttons{ class: modal? ? 'modal-footer' : '' }
  - if modal?
    %button.btn.btn-default{ type: 'button', data: { dismiss: 'modal' }, aria: { label: 'Cancel' } } Cancel
  - else
    = link_to 'Cancel', os_images_private_index_url, class: 'btn btn-default'
  -# %button{type: "submit", class: 'btn btn-primary', data: { disable_with: 'Please wait...'}} Save
