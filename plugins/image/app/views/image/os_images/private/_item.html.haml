%tr
  %td
    - if current_user.is_allowed?('image:image_get', {})
      = link_to image.name, plugin('image').send("os_images_#{@visibility}_path", image.id), data: {modal: true, cancel: 'Close'}

  %td
    - project_id_and_name(image.owner)
  %td= image.status
  %td.snug
    .btn-group
      %button.btn.btn-default.btn-sm.dropdown-toggle{ type: 'button', data: { toggle: 'dropdown' }, aria: { expanded: true } }
        %span.fa.fa-cog
      %ul.dropdown-menu.dropdown-menu-right{ role: 'menu' }
        %li= link_to 'Access Control', plugin('image').os_images_private_access_control_path(private_id: image.id), data: { modal: true }
