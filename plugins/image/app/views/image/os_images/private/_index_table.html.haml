%table.table.private_images.searchable{data:{search_input: '#search_private_images_input'}}
  %thead
    %tr
      %th Name
      %th Format
      %th Size
      %th Owning Project
      %th Status
      %th.snug
  %tbody
    - if @images.empty?
      %tr
        %td{colspan: 6} No private images found.
    - else
      = render partial: 'list', locals: { images: @images }
