.row
  %table.table
    %thead
      %tr
        %th Name
        %th Format
        %th Size
        %th.snug Status

    %tbody
      - if @images.empty?
        %tr
          %td{colspan: 4} No public imagaes found.
      - else    
        - @images.each do |image|
          = render partial: "item", locals: { image: image }
    
