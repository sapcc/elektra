- if @resources.blank?
  .bs-callout.bs-callout-info
    Nothing to report! Everything is fine.
- else
  - @resources.sort_by { |r| [ r.name, r.config.service.name ] }.each do |res|
    = render partial: 'bar_row', locals: { overview: true, resource: res }

=render partial: 'data_age'
