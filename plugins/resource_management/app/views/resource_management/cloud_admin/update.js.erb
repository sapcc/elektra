// re-render the edited row
update_resource_row(
    '<%= escape_javascript(@domain_resource.domain_id) %>',
    '<%= escape_javascript(render partial: 'details_table_row', locals: { resource: @domain_resource }) %>'
);

// if data has been changed...
<% if @cluster_resource %>
    // render usage bars
    $("#usage_bars").html("<%= escape_javascript(render partial: "details_usage_bar") %>");
    // hide alerts 
    $('.alert-danger').css('display','none');
<% end %>
