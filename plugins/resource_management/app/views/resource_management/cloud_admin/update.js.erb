// re-render the edited row
update_resource_row(
    '<%= escape_javascript(@domain_resource.id) %>',
    '<%= escape_javascript(render partial: 'details_table_row', locals: { domain_status: @domain_status }) %>'
);

// if data has been changed...
<% if @cloud_status %>
    // render usage bars
    $("#usage_bars").html("<%= escape_javascript(render :partial => "details_usage_bar") %>");
    // hide alerts 
    $('.alert-danger').css('display','none');
<% end %>
