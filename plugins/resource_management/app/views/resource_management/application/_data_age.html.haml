.row
  .col-md-10.col-md-offset-2.small.text-muted
    Usage data last updated #{data_age_as_string(@min_updated_at, @max_updated_at)}
    - if local_assigns.has_key?(:sync_now_url)
      &ndash;
      = link_to 'Sync now', sync_now_url
