<% if @services_with_error.empty? %>
  $('#set_quota_error').addClass('hide');
<% else %>
  $(".flashes").html("<%= escape_javascript(render "/resource_management/application/set_quota_error") %>");
<% end %>

//close the window
Dashboard.hideModal();
$('#resource_area').html("<%= escape_javascript(render partial: 'resource_area') %>");
