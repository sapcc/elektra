// re-render the edited row
update_resource_row(
    '<%= escape_javascript(@project_resource.id.to_s) %>',
    '<%= escape_javascript(render partial: 'details_table_row', locals: { project_resource: @project_resource }) %>'
);

// if data has been changed...
<% if @resource_status %>
    // render usage bars
    $("#usage_bars").html("<%= escape_javascript(render :partial => "details_usage_bar")%>");
    // hide alerts 
    $('.alert-danger').css('display','none');
<% end %>
