<% if @services_with_error.empty? %>
  $('#set_quota_error').addClass('hide');
<% else %>
  $(".flashes").html("<%= escape_javascript(render "set_quota_error") %>");
<% end %>

// close modal window
Dashboard.hideModal();
// reload remote inquries
PollingService.update('inquiries');
PollingService.update('projects');
