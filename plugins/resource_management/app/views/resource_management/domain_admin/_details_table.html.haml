%table.table.resource-details
  %thead
    %tr
      %th.sortable#scope_name_column 
        Project
        %i.fa.fa-sort-alpha-asc{style:'display:none'}
      %th.sortable#approved_quota_column
        Approved Quota
        %i.fa.fa-sort-amount-asc{style:'display:none'}
      %th.sortable#current_quota_column
        Current Quota
        %i.fa.fa-sort-amount-asc{style:'display:none'}
      %th.sortable#usage_column
        Usage
        %i.fa.fa-sort-amount-asc{style:'display:none'}
      %th Actions
  %tbody
    - @projects.each do |project_resource|
      = render :partial => 'details_table_row', :locals => { :project_resource => project_resource }
  
= paginate @projects, :theme => 'twitter-bootstrap-3', :remote => true

= content_for :javascripts do
  :javascript
    #{render partial: 'details_table_script.js', locals: { scope: 'admin', resource: @resource, service: @service} }

