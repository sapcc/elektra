<% if @services_with_error.empty? %>
$('#error_message').addClass('hide');
<% else %>
$('#error_message').removeClass('hide').html('There was an error when setting the approved quota in the backend services: <%= escape_javascript(@services_with_error.map(&:to_s).join(', ')) %>');
<% end %>

// close modal window
Dashboard.hideModal();
// reload remote inquries
PollingService.update('inquiries');
PollingService.update('projects');
