=content_for :main_toolbar do
  Object Storage

%p
  = link_to 'All containers', plugin('object_storage').containers_path
  &gt;
  = make_breadcrumb(@container_name, params[:path])

.toolbar
  = link_to 'Upload file', plugin('object_storage').new_object_path(@container_name, params[:path]), class: 'btn btn-primary btn-lg', data: { modal: true }
  = link_to 'Create directory', '#', class: 'btn btn-primary btn-lg disabled'

%table.table
  %thead
    %tr
      %th Object name
      %th Size
      %th Last Modified
      %th.snug
  %tbody
    - if @objects.empty?
      %tr
        %td{colspan: 4} No objects found.
    - else
      - @objects.sort_by(&:ui_sort_order).each do |object|
        = render partial: 'item', locals: { object: object }
