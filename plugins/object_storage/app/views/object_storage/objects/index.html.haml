=content_for :main_toolbar do
  Object Storage

%p
  = link_to 'All containers', plugin('object_storage').containers_path
  &gt;
  = make_breadcrumb(@container_name, params[:path])

.toolbar
  - if @objects.empty?
    = link_to 'Delete Container', plugin('object_storage').confirm_deletion_container_path(@container_name), class: 'btn btn-danger btn-lg', data: { modal: true }
  - else
    = link_to 'Empty Container', plugin('object_storage').confirm_emptying_container_path(@container_name), class: 'btn btn-danger btn-lg', data: { modal: true }
  = link_to 'Upload file', plugin('object_storage').new_object_path(@container_name, params[:path]), class: 'btn btn-primary btn-lg', data: { modal: true }
  = link_to 'Create folder', plugin('object_storage').new_folder_path(@container_name, params[:path]), class: 'btn btn-primary btn-lg', data: { modal: true }

%table.table
  %thead
    %tr
      %th Object name
      %th Size
      %th Last Modified
      %th.snug
  %tbody
    - if @objects.empty?
      %tr
        %td{colspan: 4} No objects found.
    - else
      - @objects.sort_by(&:ui_sort_order).each do |object|
        = render partial: 'item', locals: { object: object }
