=content_for :main_toolbar do
  Object Storage

.toolbar
  = link_to 'Create container', plugin('object_storage').new_container_path(), class: 'btn btn-primary btn-lg', data: {modal:true}
  = link_to 'Cluster capabilities', plugin('object_storage').capabilities_path(), class: 'btn btn-default btn-lg', data: { modal: true }

#container_list
  = render partial: 'list'


