=content_for :main_toolbar do
  Object Storage

.bs-callout.bs-callout-warning
  Object storage is not enabled for this project, yet.

= link_to 'Cancel', plugin('identity').project_path, class: 'btn btn-default'
= link_to 'Enable', plugin('object_storage').create_account_path, class: 'btn btn-primary'
