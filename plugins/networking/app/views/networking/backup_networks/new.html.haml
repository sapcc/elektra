= content_for :title do
  Create RBAC to Backup Network

= simple_form_for :backup_network, url: plugin('networking').backup_networks_path, method: :post, remote: request.xhr?, html: {data: {modal: true} } do |f|

  %div{class: modal? ? 'modal-body' : ''}

  %div.buttons{class: modal? ? 'modal-footer' : ''}
    %button.btn.btn-default{ type: 'button', data: { dismiss:'modal' }, aria: { label: 'Cancel' } } Cancel
    %button{ type: 'submit', class: 'btn btn-primary', data: { disable_with: 'Please wait...'} } Create
