%tr{id: "network_#{rbac.id}"}
  %td= rbac.action
  %td= rbac_target(rbac.target_tenant)

  %td.snug
    -# - if current_user.is_allowed?("networking:network_delete", {}) or current_user.is_allowed?("networking:network_update", {})
    -#   .btn-group
    -#     %button.btn.btn-default.btn-sm.dropdown-toggle{ type: "button", data: { toggle: "dropdown"}, aria: { expanded: true} }
    -#       %span.fa.fa-cog
    -#
    -#     %ul.dropdown-menu.dropdown-menu-right{ role:"menu"}
    -#       %li= link_to 'Delete', plugin('networking').network_access_control_path(id: rbac.id), method: :delete, data: { confirm: 'Are you sure you want to delete this access control?', ok: "Yes, delete it", confirmed: :loading_status}, remote: true
