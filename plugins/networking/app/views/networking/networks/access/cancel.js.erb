$("#new_access").empty();
// switch toolbar buttons
$("#access_toolbar").html("<%= escape_javascript(render partial: 'new_access_buttons', locals: { network_id: @network_id, network_type: @network_type} ) %>");
