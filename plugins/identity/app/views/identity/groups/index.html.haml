%table.table#groups
  %thead
    %tr
      %th Name
      %th Description
      %th ID
      %th Actions

  %tbody
    - @groups.each do |group|
      = render partial: 'group', locals: {group: group} 
