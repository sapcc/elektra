%tr
  %td= user.name
  %td= user.id
  %td.snug
    - if current_user.is_allowed?("identity:group_remove_member", {group: group}) 
      = link_to plugin('identity').group_members_remove_path(group.id, user.id), class: "btn btn-default btn-sm", method: :delete do
        %i.fa.fa-trash.fa-fw
        Remove
          
  / %td.snug
  /   - if current_user.is_allowed?("identity:group_remove_member", {group: group})
  /     .btn-group
  /       %button.btn.btn-default.btn-sm.dropdown-toggle{ type: "button", data: { toggle: "dropdown"}, aria: { expanded: true} }
  /         %span.fa.fa-cog
  /         -# %span.spinner
  /
  /       %ul.dropdown-menu.dropdown-menu-right{ role:"menu"}
  /         %li= link_to 'Remove', plugin('identity').group_members_remove_path(group.id, user.id), method: :delete
  