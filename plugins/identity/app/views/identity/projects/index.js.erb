$("#<%=params[:container_id]%>").html("<%= escape_javascript(render(partial: 'projects', locals: {projects: @projects, remote: true})) %>");
// update url
//$("#<%=params[:container_id]%>").projectsWidget('update',{"url": '<%=projects_path(page:@page)%>'})
