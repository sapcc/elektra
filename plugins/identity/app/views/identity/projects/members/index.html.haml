- if modal?
  = content_for :title do
    Authorizations
- else
  = content_for :main_toolbar do
    Authorizations

= javascript_include_tag "identity_role_assignments_widget", data: {flashescontainer: "custom", project_id: @scoped_project_id, domain_id: @scoped_domain_id}
