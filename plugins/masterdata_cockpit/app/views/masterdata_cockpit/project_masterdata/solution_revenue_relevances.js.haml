- if @solution_revenue_relevances
  $('#solution').html("#{escape_javascript(render partial: 'revenue_relevances')}");
- else
  $('#solution').html("#{escape_javascript(render partial: 'co_freestyle')}");

