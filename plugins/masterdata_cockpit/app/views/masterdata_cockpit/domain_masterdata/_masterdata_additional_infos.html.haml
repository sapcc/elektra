
- if @domain_masterdata
  .infobox
    %h4.action-heading
      Masterdata Status
    %table.table
      %tbody
        %tr
          %th
          %td
            - if @domain_masterdata.is_complete
              = icon('thumbs-up')
              Complete
            - else
              = icon('thumbs-down')
              Incomplete
              %br
              %span.label.label-warning 
                = @domain_masterdata.missing_attributes  
        %tr
          %th.snug-nowrap
            Last change:
          %td
            - if @domain_masterdata.changed_at
              - last_change = DateTime.parse(@domain_masterdata.changed_at)
              = last_change.strftime('%I:%M:%S %p %d.%B %Y')
            - else
              \-
  = render partial: "what_is_masterdata"