<%if @pool %>
$("#pool").replaceWith("<%= j(render(partial: 'loadbalancing/loadbalancers/pools/item_detail', locals: {loadbalancer: @loadbalancer, pool: @pool, listener: @listener})) %>");
$("#pool").update();
PollingService.update('<%=@loadbalancer.id%>/update_status')
MoModal.close();
<% else %>
$("#pool").remove();
PollingService.update('<%=@loadbalancer.id%>/update_status')
<% end %>