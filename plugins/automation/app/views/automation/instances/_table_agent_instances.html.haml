%table.table
  %thead
    %tr
      %th Name
      %th OS
      %th IP
      %th Agent Version
      %th Online
  %tbody
    - if instances.empty?
      %tr
        %td{colspa: 6} No instances available
    - else
      -instances.values.each do | instance |
        = render partial: 'table_agent_instances_item', locals: {instance: instance}