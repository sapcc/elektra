$(".js-search-render-area").html("<%= escape_javascript(render partial: 'table_nodes_pagination') %>");

// reload the pagination spinner
$('[data-toggle="paginationSpinner"]').initPaginationSpinner()
