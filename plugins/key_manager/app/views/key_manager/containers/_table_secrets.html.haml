.container-secrets-table
  
  %table.table
    %thead
      %tr
        %th Name
        %th Type
        %th Status
        %th.snug
    %tbody
      - if secrets.empty?
        %tr
          %td{colspan: 4} No secrets available
      - else
        -secrets.each do | secret |
          = render partial: 'table_secrets_item', locals: {secret: secret}