$("table.volumes").prepend("<%= j(render(partial: 'block_storage/volumes/item', locals: {volume: @volume, target_state: @target_state, servers: @servers })) %>");
$("#volume_<%=@volume.id%>").update()
MoModal.close();
