$("#volume_<%=@volume.id%>").replaceWith("<%= j(render(partial: 'block_storage/volumes/item', locals: {volume: @volume, target_state: @target_state})) %>");
$("#volume_<%=@volume.id%>").update()
MoModal.close();
