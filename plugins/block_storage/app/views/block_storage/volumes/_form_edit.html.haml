= simple_form_for @volume, url: plugin('block_storage').volume_path(@volume.id), method: :put, remote: request.xhr?, html: {data: {modal: true} } do |f|

  %div{class: modal? ? 'modal-body' : ''}
    - unless @volume.errors["message"].blank?
      %p.alert.alert-error= @volume.errors.full_messages.to_sentence

    .row
      .col-md-6
        %fieldset
          = f.input :name
          = f.input :description

  %div.buttons{class: modal? ? 'modal-footer' : ''}
    - if modal?
      %button.btn.btn-default{type:"button", data: {dismiss:"modal"}, aria: {label: "Cancel"}} Cancel
    - else
      = link_to "Cancel", volumes_url(), class: 'btn btn-default'
    %button{type: "submit", class: 'btn btn-primary', data: { disable_with: 'Please wait...'}} Change

