= content_for :title do
  New Alarm Definition

= simple_modal_form_for @alarm_definition, submit_action: 'Create', url: plugin('monitoring').alarm_definitions_path, method: :post, cancel_url: plugin('monitoring').alarm_definitions_path, html: { class: 'form-horizontal' }, wrapper: :horizontal_form, autofocus: 'alarm_definition_name' do |f|
  #backend_error
  = f.input :name, placeholder: 'Enter name', :input_html => {:"data-focus" => 'autofocus'}
  = f.input :description, placeholder: 'Enter Description'
  = f.input :expression, placeholder: 'Enter Expression', icon_hint: "If the expressions is true, a notification will be sent. See #{link_to 'Description', 'https://github.com/openstack/monasca-api/blob/master/docs/monasca-api-spec.md#alarm-definition-expressions'} how to write a expression"
  = f.input :severity, collection: ['Low','Medium','High','Critical'], selected:'Low', icon_hint: 'Defines the severity level for the alarm'
  = f.input :match_by, icon_hint: 'Comma seperated list of dimensions, A separate Alarm will be created for every single dimension value'
  %hr

