%table.table
  %thead
    %tr
      %th Name
      %th Severity
      %th Description
      %th Notification
      %th.snug
  %tbody
    - if @alarm_definitions.empty?
      %tr
        %td{colspan: 4} No alarm definitions found.
    - else
      -# TODO: add pagination
      - @alarm_definitions.sort_by(&:name).each do |alarm_definition|
        = render partial: 'definitions_row', locals: { alarm_definition: alarm_definition } 

