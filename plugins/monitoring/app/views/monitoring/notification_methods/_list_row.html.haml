%tr
  %td= notification_method.name
  %td 
    - if notification_method.type == "EMAIL"
      %span{title: "email notification_method", style: "cursor:help"}
        = icon('envelope-o')
    - else
      %strong ?
  %td= notification_method.address
  %th.snug
    .btn-group
      %button.btn.btn-default.btn-sm.dropdown-toggle{ type: 'button', data: { toggle: 'dropdown' }, aria: { expanded: true } }
        %span.fa.fa-cog

      %ul.dropdown-menu.dropdown-menu-right{ role: 'menu' } 
        %li= link_to 'Show', plugin('monitoring').notification_method_path(notification_method.id), data: { modal: true }
        %li= link_to 'Edit', plugin('monitoring').edit_notification_method_path(notification_method.id), data: { modal: true }
        %li= link_to 'Delete', plugin('monitoring').notification_method_path(notification_method.id), method: :delete, data: { confirm: "Are you sure?" }

