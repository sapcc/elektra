- if modal?
  = content_for :title do
    Backend error
- else
  = content_for :main_toolbar do
    Monitoring

%div{ class: modal? ? 'modal-body' : '' }
  %p An error was encountered while executing an Monitoring (Monasca) API call:
  %p.alert.alert-error= @exception_msg
