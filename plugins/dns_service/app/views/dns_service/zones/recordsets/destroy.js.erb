<% if @deleted %>
  <% if @action_from_show %>
    window.location = "<%= plugin('dns_service').zone_path(@zone_id) %>";
  <% else %>
    $('table tr#recordset_<%=params[:id]%>').remove();
  <% end %>
<% end %> 