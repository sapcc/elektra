<% if @action_from_show %>
  $("#item_details").replaceWith("<%= j(render(partial: 'compute/instances/item_details')) %>");
<% else %>
  $('tr#instance_<%=@instance.id%>').replaceWith("<%= j(render(partial: 'compute/instances/item', locals: { instance: @instance })) %>");
<% end %>
MoModal.close();
