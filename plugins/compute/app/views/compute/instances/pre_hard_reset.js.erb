//close the create request window
Dashboard.hideModal();

// trigger hard reset
$.post("<%=plugin('compute').hard_reset_instance_path({id:@instance.id})%>");

//close the create request window
Dashboard.hideModal();

$('#console').html("<div class='countdown'/>");
$('.countdown').html('Hard Reset was triggered 🚀<br>Please wait a few secconds and reload the Web Console');	