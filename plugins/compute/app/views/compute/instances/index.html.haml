= render "intro"

%p Deliverables next-generation authentic empower distributed empower facilitate infrastructures innovative wikis grow dynamic. Morph widgets integrate, rich scale interfaces world-class create, e-markets supply-chains compelling applications visualize iterate out-of-the-box integrateAJAX-enabled viral.

- if @scoped_project_id
  - if current_user.is_allowed?("identity:instance_create", {instance: {user_id: current_user.id} })
    .toolbar
      = link_to 'Create new', new_instance_path(domain_id: @scoped_domain_fid, project_id: @scoped_project_fid), data: {modal: true}, class: 'btn btn-primary btn-lg'

  %table.table
    %thead
      %tr
        %th Name
        %th OS
        %th IP
        %th Size
        %th Power state
        %th Status
        %th.snug
    %tbody
      - if @instances.empty?
        %tr
          %td{colspa: 5} No instances available
      - else
        - @instances.each do | instance |
          = render partial: 'item', locals: {instance: instance}
- else
  = render "application/projects_list"
