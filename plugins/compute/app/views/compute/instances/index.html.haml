= render "intro"

- if @scoped_project_id
  - if current_user.is_allowed?("compute:instance_create", {instance: {user_id: current_user.id} })
    .toolbar
      = link_to 'Create new', plugin('compute').new_instance_path, data: {modal: true}, class: 'btn btn-primary btn-lg'

  %table.table
    %thead
      %tr
        %th Name
        %th OS
        %th IP
        %th Size
        %th Power state
        %th Status
        %th.snug
    %tbody
      - if @instances.empty?
        %tr
          %td{colspa: 5} No instances available
      - else
        - @instances.each do | instance |
          = render partial: 'item', locals: {instance: instance}
- else
  = render "application/projects_list"
