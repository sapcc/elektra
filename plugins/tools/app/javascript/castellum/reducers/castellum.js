import * as constants from '../constants';

const initialState = {
}

export const castellum = (state, action) => {
  if (state == null) {
    state = initialState;
  }

  switch (action.type) {
    default: return state;
  }
}
