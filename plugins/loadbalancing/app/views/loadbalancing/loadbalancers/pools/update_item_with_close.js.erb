<%if @pool %>
$("#pool").replaceWith("<%= j(render(partial: 'loadbalancing/loadbalancers/pools/item_detail', locals: {pool: @pool, loadbalancer: @loadbalancer})) %>");
$("#pool").update();
MoModal.close();
<% else %>
$("#pool").remove();
<% end %>