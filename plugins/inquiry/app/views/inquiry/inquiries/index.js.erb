$("#<%=params[:container_id]%>").html("<%= escape_javascript(render(partial: 'index', locals: {inquiries: @inquiries, remote: true})) %>");
// update url
$("#<%=params[:container_id]%>").inquiryWidget('update',{"url": '<%=inquiries_path(page:@page)%>'})
