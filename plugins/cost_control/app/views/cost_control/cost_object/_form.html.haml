- types = CostControl::ProjectMetadata::COST_OBJECT_TYPES
= form.input :cost_object_type, label: long_labels ? 'Cost Object Type' : 'Type', collection: types.keys, label_method: -> (k) { types[k] }
= form.input :cost_object_id, label: long_labels ? 'Cost Object ID' : 'ID'
.row
  .help-block.col-sm-8.col-sm-offset-4
    A cost object is an element that we can bill for services rendered.
  -# TODO: monsoon2 has a link to "Learn more about pricing." here
