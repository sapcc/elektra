{
  "member": "role:member or role:Member",
  "admin": "role:admin",

  "docker_registry:entry_list": "rule:admin or rule:member",
  "docker_registry:entry_get": "rule:admin or rule:member",
  "docker_registry:entry_create": "rule:admin or rule:member",
  "docker_registry:entry_delete": "rule:admin or rule:member",
  "docker_registry:entry_update": "rule:admin or rule:member"
}
