<%
  PluginsManager.available_plugins.each do |plugin|
    path = resolve("#{plugin.name}/application") rescue nil

    if path
      %><%=".#{plugin.name} {  @import \"#{plugin.name}/application\"; }" %>
      <%
      #".#{plugin.name} { @import \"#{plugin.name}/application\"; }\n" 
    end
  end  
%>


