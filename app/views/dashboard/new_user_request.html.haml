= content_for :page_lead do
  .jumbotron
    .container
      %h2
        Welcome
        = current_user.full_name
      %p Please accept the terms of use for this domain and request access to it. Once you have accepted the terms a request to the responsible Domain administrator will be created. You will receive an email on your request status after approval or rejection.


= form_tag main_app.register_request_path, method: :post do
  .row
    .col-sm-8
      .form-group
        %label Terms and Conditions
        .scrollable-text
          :markdown
            #{Settings.terms}

    .col-sm-4
      .form-group
        %label User
        %input.form-control{type: "text", placeholder: "#{current_user.full_name} (#{current_user.name})", readonly: "readonly"}

      .form-group
        %label Domain
        %input.form-control{type: "text", placeholder: @scoped_domain_name, readonly: "readonly"}


      .form-group
        .checkbox
          %label
            = check_box_tag 'terms_of_use', 'accepted', false, id: "accept_tos"
            I have read and agree to the terms of use

      .form-group
        = submit_tag "Request access", class: "btn btn-primary btn-lg", id: "register-button", disabled: true
