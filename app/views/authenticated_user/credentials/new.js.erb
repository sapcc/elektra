$("#credentialForm .modal-body").html("<%= escape_javascript(render partial: 'form', locals: {forms_credential: @forms_credential} ) %>").triggerHandler('contentLoaded');; 
$($("#credentialForm .modal-body").find(".dynamic-form-trigger")).change(Dashboard.hideRevealFormParts);
