$("#credential_form").html("<%= escape_javascript(render partial: 'form', locals: {forms_credential: @forms_credential} ) %>"); 
$($("#credential_form").find(".dynamic-form-trigger")).change(Dashboard.hideRevealFormParts);
