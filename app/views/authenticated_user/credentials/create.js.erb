// close overlay
$($("#credential_form").closest('.modal')).modal('hide')

// // add created item to table
// $('#credentials_table').append("<%= escape_javascript(render partial: 'item', locals: {credential: @forms_credential }) %>");
// // reset credential form
// $("#credential_form").html("<%= escape_javascript(render partial: 'form', locals: {forms_credential: services.identity.forms_credential}) %>");
location.reload();