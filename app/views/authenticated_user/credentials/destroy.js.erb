<% if @forms_credential.errors.empty? %>
  $("#credential_<%=@forms_credential.id%>").remove();
<% else %>
  $("#credential_<%=@forms_credential.id%>").removeClass('deleting');
  waitingDialog.showError('<%=@forms_credential.errors.full_messages.to_sentence%>');
<% end %>