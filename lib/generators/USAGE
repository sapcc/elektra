Description:
  This generator creates a dashboard plugin.
  For example, to create a react plugin use the option --react. Should it be
  a pure javascript project without the controller component then use the option
  --no-mountable.

Example:
    rails generate dashboard_plugin NAME

    This will create:
        * a new folder under plugins/
        * a properly configured plugin
        * a mounted and runnable plugin (if mountable)
        * a service_layer (if the option --service_layer is used)
        * specs for controllers
